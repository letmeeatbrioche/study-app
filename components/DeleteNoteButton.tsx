import React from 'react'

type Props = {}

const DeleteNoteButton = (props: Props) => {
  // Function: delete note card from database and category
  return (
    <button type='button'>DeleteNoteButton</button>
  )
}

export default DeleteNoteButton